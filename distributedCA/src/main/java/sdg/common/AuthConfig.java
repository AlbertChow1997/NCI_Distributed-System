package sdg.common;

public final class AuthConfig {
    // This class is only to keep the authentication values in one place.
    // In class examples, metadata is often used for auth information, so I do the same here.
    public static final String API_KEY_HEADER = "x-api-key";
    public static final String API_KEY_VALUE = "climate-action-2026";

    private AuthConfig() {
    }
}
